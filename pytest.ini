[pytest]
markers =
    unit: Unit tests (fast, no GPU required)
    integration: Integration tests (slow, may require GPU)
    gpu: Tests that require GPU
    slow: Tests that take a long time to run
    download: Tests that download data from the internet
    ddp: Multi-GPU DDP tests (requires srun with >=2 GPUs)
    distributed: Distributed tests using torch.distributed (gloo/CPU OK; spawn multiple procs)
    jax: JAX/Flax-NNX backend tests (skipped when jax is not installed)

testpaths = stable_pretraining/tests

# Test discovery patterns
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# Coverage settings
[coverage:run]
source = stable_pretraining
omit =
    */tests/*
    */test_*
